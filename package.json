
{
  "name": "smart-broadband-fullstack",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run install-dependencies && npm run build:client",
    "build:client": "cd client && npm run build",
    "install-dependencies": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "render-build": "npm run install-dependencies && npm run build:client"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}

